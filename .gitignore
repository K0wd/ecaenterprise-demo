# Dependencies
node_modules/

# Secrets — never commit real credentials
.env

# Playwright / playwright-bdd generated output
.features-gen/
playwright-report/
test-results/
blob-report/
playwright/.cache/

# CI Pages assembly (generated in the workflow)
public/
prev-history.json
